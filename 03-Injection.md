# 💉 A03: Injection

---

## 📌 1. Definition (التعريف الاحترافي)

هو إدخال بيانات خبيثة (malicious input) يتم تفسيرها كأوامر من قبل السيرفر مثلا يقوم المهاجم بإرسال نص (مثل رموز SQL أو أوامر System) ,  
السيرفر لا يستطيع التمييز بين "البيانات العادية" وبين "الأوامر البرمجية", بدلاً من أن يقوم السيرفر بالبحث عن اسم المستخدم فقط، يقوم بتنفيذ الأمر الخبيث

---

## 🧪 2. Types (أنواع الـ Injection)

---

# 🧪 أولاً: SQL Injection | حقن قواعد البيانات

## 💡 الفكرة الأساسية

الموقع يأخذ مدخلاتك ويضعها مباشرة داخل الاستعلام (Query) دون فحص، مما يسمح لك بتغيير منطق الاستعلام.

---

## 🔎 المثال التطبيقي (PoC)

### 📌 الاستعلام الطبيعي في السيرفر

```sql
SELECT * FROM users WHERE username = 'USER_INPUT' AND password = 'PASSWORD_INPUT'
```

---

### 📌 ما يدخله المهاجم (Payload)

في خانة اليوزر نيم نضع:

```sql
' OR 1=1 --
```

---

### 📌 كيف يراها السيرفر بعد الحقن

```sql
SELECT * FROM users WHERE username = '' OR 1=1 --' AND password = '...'
```

---

## 💥 ماذا حدث؟

1. **`'`** : قمنا بإغلاق حقل النص الخاص باليوزر نيم.
    
2. **`OR 1=1`**: شرط منطقي صحيح دائماً، يجعل السيرفر يتجاهل صحة اليوزر والباسورد.
    
3. **`--`**: علامة تعليق (Comment) تخبر السيرفر بتجاهل باقي الاستعلام (تجاهل جزء الباسورد تماماً).

---

### ✅ النتيجة

تسجيل دخول ناجح كأول مستخدم في القاعدة (غالباً يكون الـ Admin) بدون كلمة مرور.

---

## 🧠 كيف تفكر في الـ SQL Injection؟

قبل أن تبدأ بالحقن، اسأل نفسك:

- **هل فيه Input؟** هل يوجد حقل بحث، فورم دخول، أو حتى معاملات (Parameters) في الرابط؟
    
- **هل يتعامل مع الـ DB؟** هل هذا المدخل يُستخدم لجلب بيانات من قاعدة البيانات؟
    
- **هل أقدر أكسر الاستعلام؟** لو وضعت علامة التنصيص (`'`) هل سيظهر خطأ (SQL Error)؟ إذا ظهر الخطأ، فالباب مفتوح!

---

# 🧪 ثانيًا: XSS (Cross-Site Scripting)

## 💡 الفكرة الأساسية

بدلاً من استهداف قاعدة البيانات، أنت تستهدف **المستخدم** (الضحية).  
تقوم بحقن كود **JavaScript** خبيث، وعندما يزور الضحية الصفحة، يشتغل الكود في متصفحه تلقائياً.

---

## 🔎 مثال بسيط (PoC)

في خانة "التعليقات" أو "البحث"، اكتب:

```html
<script>alert('Hacked')</script>
```

💥 إذا ظهرت نافذة التنبيه، فالموقع مصاب.

---

## 📌 أنواع الـ XSS

### 🔹 Stored XSS

الكود يُحفظ في قاعدة البيانات (مثل تعليق) ويظهر لكل من يزور الصفحة. (الأخطر 🔥).

---

### 🔹 Reflected XSS

الكود لا يُحفظ، بل "ينعكس" من الرابط (URL).  
يجب أن ترسل الرابط للضحية ليضغط عليه.

---

### 🔹 DOM-based XSS

الثغرة تكون داخل كود الجافاسكريبت الخاص بالصفحة نفسها في جهة العميل (Client-side).

---

## 🧠 كيف تفكر في الـ XSS؟

- **هل الموقع يعرض المدخلات؟** لو كتبت "علي" في البحث، هل تظهر كلمة "نتائج البحث عن: علي" في الصفحة؟
    
- **هل فيه فلترة؟** هل يمنع الموقع استخدام الأقواس `< >`؟
    
- **هل أقدر أشغل JS؟** هل يمكنني إقناع المتصفح بتنفيذ أمر بسيط مثل `alert` أو سرقة الـ `document.cookie`؟

---

# 🧪 ثالثاً: Command Injection (حقن الأوامر)

## 💡 الفكرة الأساسية

تحدث عندما يقوم الموقع بتمرير مدخلات المستخدم مباشرة إلى "شاشة الأوامر" (Terminal/Shell) الخاصة بالسيرفر لتنفيذ مهمة معينة (مثل فحص اتصال أو تحويل صورة).

---

## 🔎 المثال التطبيقي (PoC)

### 📌 الوظيفة الطبيعية

موقع يوفر أداة عمل `ping` لسيرفر معين.

---

### 📌 المدخل المتوقع

```bash
8.8.8.8
```

---

### 📌 ما يفعله المهاجم

يستخدم "مُفصلات الأوامر" (Command Separators) مثل:

```bash
;
&&
|
```

---

### 📌 المدخل الخبيث

```bash
8.8.8.8 ; ls
```

---

## 💥 ماذا حدث؟

السيرفر قام بتنفيذ أمر الـ Ping أولاً، وبسبب وجود الفاصل `;` قام بتنفيذ الأمر الثاني `ls` الذي يعرض الملفات الموجودة على السيرفر.

> إذا كان السيرفر بنظام Linux، يمكنك كتابة:
>
> ```bash
> cat /etc/passwd
> ```
>
> لقراءة بيانات المستخدمين!

---

## 🧠 كيف تفكر كمخترق؟ (Mindset)

عندما تجد ميزة في الموقع تتفاعل مع "نظام التشغيل" (مثل رفع ملفات، ضغط ملفات، فحص شبكة)، اسأل نفسك:

- **🛠️ ما هي الأداة التي تعمل في الخلفية؟** (هل الموقع يستخدم برنامج خارجي لتنفيذ هذه المهمة؟).
    
- **🖇️ هل يمكنني دمج أمر إضافي؟** (ماذا لو وضعت `;` أو `&`؟ هل سيتوقف النظام أم سينفذ ما بعدها؟).
    
- **👤 ما هي صلاحياتي؟** (لو نفذت أمر `whoami` هل أنا مستخدم عادي أم `root`؟).

---

## 🛠️ 3. Discovery (كيف تكتشف الـ Injection؟)

لكي تعرف إذا كان الموقع "قابلاً للحقن"، اتبع هذه الخطوات العملية:

---

### 🧪 تجربة الرموز الكاسرة (Fuzzing Characters)

قم بإدخال هذه الرموز في أي حقل تجده (البحث، اليوزر نيم، روابط الـ URL):

```text
'   "
<   >
;   &&
|
```

---

### ⚠️ مراقبة الأخطاء (Error Monitoring)

إذا أدخلت رمزاً وظهر لك خطأ من نوع:

```text
SQL Syntax Error
Internal Server Error
```

فهذا معناه أن السيرفر "تأثر" بمدخلاتك، وأنه لا يقوم بعملية فلترة صحيحة.

---

### 📡 استخدام Burp Suite

- راقب كيف يتم إرسال البيانات (GET أو POST).
    
- استخدم الـ **Repeater** لتكرار الطلب مع تغيير الرموز في كل مرة حتى تكتشف الثغرة.
    
- استخدم الـ **Intruder** لعمل فحص تلقائي (Fuzzing) باستخدام قوائم Payload جاهزة.

---

### 👁️ فحص استجابة الصفحة

في حالات الـ **XSS**، ابحث في "سورس كود" الصفحة (View Source) لترى هل الرموز التي أدخلتها ظهرت كما هي أم تم تشفيرها؟

---

## 🛡️ 4. Remediation (كيف تُصلح كل أنواع الـ Injection؟)

### ❌ لا تدمج أبداً

لا تضع مدخلات المستخدم مباشرة في الاستعلامات أو الأوامر.

---

### ✅ استخدم الـ Parameterized Queries

في الـ SQL، استخدم الاستعلامات المُجهزة (Prepared Statements).

---

### 🧹 الـ Validation والـ Sanitize

نظف المدخلات من الرموز الخطيرة مثل:

```text
' , ; < > & |
```

---

### 🔒 مبدأ الصلاحيات الأقل (Least Privilege)

اجعل قاعدة البيانات أو السيرفر يعمل بأقل صلاحيات ممكنة لتقليل الضرر في حال حدوث اختراق.

---
