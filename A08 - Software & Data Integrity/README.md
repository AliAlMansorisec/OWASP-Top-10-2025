# 🧬 A08: Software and Data Integrity Failures

## 📌 1. Definition

هي ثغرات تحدث عندما يعتمد النظام على أكواد، تحديثات، أو بيانات من مصادر خارجية دون التحقق من سلامتها (Integrity). هذا الفشل يسمح للمهاجم بحقن أكواد خبيثه , و تعديل التحديثات البرمجية، أو التلاعب بالبيانات الحساسة أثناء انتقالها أو معالجتها، مما يؤدي إلى اختراق كامل للنظام

النزاهة تعني أن الشيء الذي أرسلته هو نفسه الذي وصل، دون أن يلمسه أحد في الطريق. الثغرة تحدث عندما يفشل النظام في التأكد من:

1. **البرمجيات (Software):** هل هذا التحديث فعلاً من الشركة الرسمية؟ أم مخترق وضعه في الطريق؟
    
2. **البيانات (Data):** هل البيانات التي وصلت للسيرفر هي التي أرسلها المستخدم؟ أم تم التلاعب بها وتغيير قيمها؟

---

## 🧠 2. Real-World Analogy (مثال واقعي لفهم الثغرة)

تخيل أنك تريد تحميل **لعبة مشهورة** 🎮 من الإنترنت:

- **الوضع الطبيعي:** تذهب للموقع الرسمي، تضغط تحميل، وتلعب وأنت مطمئن.
    
- **ثغرة الـ Integrity (فشل النزاهة):**
    
    - أنت وجدت رابطاً للعبة في منتدى غير معروف.
        
    - **السؤال:** هل هذه هي النسخة الأصلية؟ أم أن هناك "هكر" قام بفك ضغط ملفات اللعبة، وضع بداخلها "فيروس" 😈 يسرق صورك وملفاتك، ثم أعاد ضغطها ورفعها مرة أخرى؟
        
- **❌ لو ما تأكدت من "سلامتها" (Integrity):** أنت هنا تثق بالملف لمجرد أن اسمه "اسم اللعبة"، لكنك لم تفحص **التوقيع الرقمي** للشركة المصنعة. بمجرد تشغيل اللعبة، أنت فعلياً سمحت للمخترق بالدخول لجهازك "بإرادتك".

---

## 🧩 3. Types (أشهر الأنواع)

### 1. تحميل مكتبات من مصدر غير موثوق

استدعاء ملفات `JS` أو مكتبات من روابط خارجية (CDNs) غير مؤمنة.

- **💥 الخطر:** لو تم اختراق ذلك الرابط، سيتم حقن كود خبيث في موقعك فوراً.
    
- **المثال:**

```html
<script src="http://unknown.com/script.js"></script>
```

---

### 2. عدم التحقق من سلامة الملفات (Missing Integrity Check)

تحميل ملفات أو تحديثات بدون التأكد من بصمتها الرقمية.

- **❌ لا يوجد:**
    
    - `Hash verification`
        
    - `Digital Signature`

---

### 3. CI/CD Pipeline غير آمن

المخترق لا يهاجم الكود الخاص بك، بل يهاجم "آلة البناء" (Build Server).

- **💥 الخطر:** يتم حقن الكود الخبيث تلقائياً أثناء عملية الـ `build` قبل أن يصل الموقع للجمهور.

---

### 4. تحديثات غير موقعة (Unsigned Updates)

أن يقبل النظام أي ملف تحديث ويركبه دون التأكد من أنه موقع من المبرمج الأصلي.

---

### 5. Insecure Deserialization (النوع الأخطر)

عندما يستقبل الموقع بيانات "معقدة" (Objects) ويحولها لكود ليفهمها السيرفر.

- **💥 الخطر:** المخترق يرسل "Object مفخخ" يؤدي لتنفيذ أوامر (RCE) بمجرد محاولة السيرفر قراءته.

---

## 🔥 4. Impact (ليش هي خطيرة؟)

خطورة هذه الثغرة تكمن في أنها تكسر **"سلسلة الثقة"** (Chain of Trust) بالكامل:

- **💉 تسمح بحقن كود خبيث (Malicious Code Injection):** المخترق يمكنه استبدال ملفات برمجية سليمة بأخرى تحتوي على "أبواب خلفية" (Backdoors). بمجرد تشغيل النظام للكود، يصبح المخترق هو المتحكم الخفي.
    
- **🔄 تحديثات مزيفة (Fake Updates):** تخيل أن موقعك يسحب تحديثاً تلقائياً من سيرفر خارجي، والمخترق نجح في اعتراض هذا المسار؛ هنا سيقوم موقعك بتحميل "الفيروس" بنفسه ظناً منه أنه "تحديث أمني".
    
- **🖥️ تنفيذ أوامر بدون علمك (Remote Code Execution - RCE):** هذه هي النتيجة النهائية الأبشع. يمكن للمهاجم إرسال بيانات "مفخخة" للسيرفر، وعندما يحاول السيرفر قراءتها، تنفجر وتنفذ أوامر (مثل مسح قاعدة البيانات أو سرقة ملفات الـ Environment) وكأنها أوامر من صاحب الموقع نفسه.

---

## 🛠️ 5. Discovery (كيف تكتشفها؟)

عملية الفحص هنا تتطلب نظرة فاحصة لكل ما يتم تحميله "خلف الكواليس":

### 🔍 راقب الـ Scripts والـ Dependencies

افحص سورس كود الصفحة (View Source) وابحث عن الروابط الخارجية.

- _اسأل:_
    
    - هل الموقع يحمل ملفات من روابط غير معروفة؟
        
    - هل الروابط تستخدم `HTTP` بدلاً من `HTTPS`؟

---

### 🧐 شوف هل فيه Integrity Check؟

عندما تجد رابط مكتبة خارجية (مثل Bootstrap أو jQuery)، ابحث عن خاصية `integrity`.

- **المثال الصحيح:**

```html
<script 
  src="https://cdn.example.com/library.js"
  integrity="sha384-..."
  crossorigin="anonymous">
</script>
```

- إذا كانت هذه الخاصية **غائبة**، فالموقع معرض لهجوم استبدال الملفات.

---

### 🧪 حلل الـ Deserialization

راقب الطلبات (Requests) في **Burp Suite**. ابحث عن بيانات تبدو "غريبة" أو مشفرة بـ `Base64`.

- جرب التلاعب بهذه البيانات وإرسالها.
    
- إذا رد السيرفر بخطأ برمجي مفصل، فقد يكون عرضة للحقن.

---

### 📦 فحص سلاسل التوريد (Supply Chain)

استخدم أدوات فحص المكتبات لمعرفة ما إذا كانت المكونات المستخدمة تحتوي على ثغرات:

- `npm audit`
    
- `Snyk`
    
- `OWASP Dependency-Check`

---

## 🛡️ 6. Remediation (كيف تُصلح؟)

لبناء نظام لا يقبل التزوير، يجب تطبيق مبدأ **"تحقق ثم ثق"**:

### ✅ استخدام مصادر موثوقة (Trusted Sources)

لا تحمل مكتبات أو إضافات من مواقع مشبوهة.

- استخدم المصادر الرسمية فقط:
    
    - `npm`
        
    - `PyPI`
        
    - `NuGet`

---

### 🛡️ بصمة الكود - Subresource Integrity (SRI)

عند استدعاء أي ملف JavaScript أو CSS من CDN، أضف خاصية `integrity`.

> هذه الخاصية تجعل المتصفح يرفض تشغيل الملف إذا تم تغيير حرف واحد فيه.

### ✅ المثال الآمن:

```html
<script 
  src="https://example.com/library.js"
  integrity="sha384-H6..."
  crossorigin="anonymous">
</script>
```

---

### ✍️ توقيع التحديثات (Digital Signatures)

يجب أن تكون التحديثات البرمجية موقعة رقمياً.

- النظام يجب أن يتحقق من التوقيع قبل تثبيت أي تحديث.
    
- أي تحديث مزيف يجب رفضه فوراً.

---

### 🏗️ تأمين الـ CI/CD Pipeline

- حماية أدوات البناء مثل:
    
    - `Jenkins`
        
    - `GitHub Actions`
        
    - `GitLab CI`
        
- تقييد الوصول لبيئة البناء.
    
- فحص الكود تلقائياً أثناء عملية الـ Build.

---

### 🚫 التحقق من البيانات (Secure Deserialization)

لا تقم بتحويل البيانات القادمة من المستخدم إلى Objects مباشرة.

- استخدم صيغ آمنة مثل:
    
    - `JSON`
        
- قم بعمل Validation صارم لكل البيانات قبل معالجتها.

---

## 🧠 7. Attacker Mindset (كيف تفكر كمخترق؟)

في هذه الثغرة، تفكيرك يتجاوز الكود البرمجي ليصل إلى **"رحلة البيانات"**.

- **🌐 هل الكود اللي يتحمل موثوق؟**
    
    "الموقع يستخدم مكتبة من `cdn.com`؛ هل الرابط مؤمن بـ `HTTPS`؟ هل يستخدمون خاصية الـ `integrity` للتأكد من أن الملف لم يتغير؟ لو استطعت السيطرة على هذا الرابط، سأسيطر على آلاف المواقع التي تستخدمه."

- **🛡️ هل في تحقق من الملفات؟**
    
    "عندما يقوم النظام بتحديث نفسه أو تحميل Plugin جديد، هل يقارن بصمته الـ `Hash`؟ إذا كان النظام يثبت أي شيء يصل إليه، سأرسل له تحديثاً يحتوي على Backdoor."

- **💉 هل أقدر أحقن كود في النظام؟**
    
    "الموقع يرسل بيانات مشفرة أو معقدة داخل الـ Requests؛ ماذا لو تلاعبت بهذه البيانات وأرسلت كوداً برمجياً بدلاً من البيانات المتوقعة؟"

- **🏗️ هل في CI/CD ضعيف؟**
    
    "هل بيئة البناء محمية؟ هل الوصول محدود؟ لو استطعت الوصول لها، سأحقن الكود الخبيث قبل أن يصل الموقع للمستخدمين."

---
